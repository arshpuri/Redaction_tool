presidio-analyzer==2.2.359
spacy>=3.7,<3.8
phonenumbers>=8.13
python-docx>=1.1
pdfplumber>=0.11
PyMuPDF>=1.24
Faker>=25.0

# One-time local model download (not a runtime call). en_core_web_lg (not
# the smaller sm model) is what detectors.py actually configures Presidio's
# NLP engine to use, for materially better PERSON NER accuracy:
#   python -m spacy download en_core_web_lg
